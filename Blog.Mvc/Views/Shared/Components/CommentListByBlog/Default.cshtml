@model IEnumerable<DakaBlog.Mvc.EntityLayer.Concrete.Comment>

<div class="comment-top">
	<h4>Yorumlar</h4>
	@*@foreach (var item in Model)
	{
		if (item.BlogId == null)
		{
			<div class="media">
				<img src="~/CoreBlogTema/images/t1.jpg" alt="" class="img-fluid" />
				<div class="media-body">
					<h5 class="mt-0">@item.CommentUserName</h5>
					<p>
						@item.CommentContent
					</p>
				</div>
			</div>
		}
		else
		{
			<p>Yorum yok ilk sen ol</p>
		}
	}*@
	@if (Model.Count() == 0)
	{
		<h2>Daha önce yorum yapılmamış.İlk yorumu sen yaz</h2>
	}
	else
	{
		@foreach (var item in Model)
		{
			<div class="media">
				<img src="~/CoreBlogTema/images/t1.jpg" alt="" class="img-fluid" />
				<div class="media-body">
					<h5 class="mt-0">@item.CommentUserName</h5>
					<p>  @item.CommentContent  </p>
				</div>
			</div>
		}
	}
</div>